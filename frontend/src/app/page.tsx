"use client";

import { Hero } from "@/components/ui/animated-hero";

export default function HomePage() {
  return (
    <section className="relative flex flex-col items-center justify-center min-h-screen bg-transparent text-black">
      <Hero />
    </section>
  );
}
